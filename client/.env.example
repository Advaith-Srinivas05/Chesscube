# Origin of the Express API (no trailing slash), used by the Socket.IO connection.
# REST calls always go to the relative /api path (Vite proxy in dev, Vercel rewrite in production).
VITE_API_URL=http://localhost:3001

# Google Identity Services OAuth client id (same value as the server's GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=
